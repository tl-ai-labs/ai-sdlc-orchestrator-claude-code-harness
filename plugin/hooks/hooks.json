{
  "hooks": {
    "PostToolUse": [
      {
        "_comment": "Regex, not literal: Claude Code namespaces plugin MCP tools as mcp__plugin_multi-model-orchestrator_gemini-flash-server__..., while a clone's .mcp.json registers them bare as mcp__gemini-flash-server__.... Optional group matches both.",
        "matcher": "mcp__(plugin_multi-model-orchestrator_)?gemini-flash-server__execute_with_model",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/telemetry.sh"
          }
        ]
      }
    ]
  }
}
